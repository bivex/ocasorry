(* Sail ISA: LicenseCascade_4vISA  tier=1  imm12  GF_poly=0x1B  ROL=15
*  Generated by OcaSorry Polymorphic ISA Synthesizer *)
default Order dec
$include <prelude.sail>
type regidx_LicenseCascade_4vISA = bits(5)
type imm_LicenseCascade_4vISA    = bits(12)
register LicenseCascade_4vISA_R  : vector(16, dec, bits(32))
register LicenseCascade_4vISA_PC : bits(32)
union LicenseCascade_4vISA_ast = {
    V_BFVR : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_MZMZ : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_QNQN : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_DSGH : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_GHZW : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_XBGH : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_GHFN : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    V_VRJR : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    I_JRKX : (regidx_LicenseCascade_4vISA, imm_LicenseCascade_4vISA),
    M_CYFN : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    L_PLTK : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    R_TKZW : (regidx_LicenseCascade_4vISA),
    B_XBKX : (regidx_LicenseCascade_4vISA, regidx_LicenseCascade_4vISA),
    J_MZQN : (bits(32)),
}
val LicenseCascade_4vISA_decode : bits(32) -> option(LicenseCascade_4vISA_ast)
function LicenseCascade_4vISA_decode(inst) = {
    let f6  : bits(6) = inst[31..26];
    let vs2 : bits(5) = inst[24..20];
    let vs1 : bits(5) = inst[19..15];
    let vd  : bits(5) = inst[11..7];
    let imm : bits(12) = inst[11..0];
    match f6 {
        40 => Some(V_BFVR(vd, vs1, vs2)),
        2 => Some(V_MZMZ(vd, vs1, vs2)),
        23 => Some(V_QNQN(vd, vs1, vs2)),
        22 => Some(V_DSGH(vd, vs1, vs2)),
        10 => Some(V_GHZW(vd, vs1, vs2)),
        35 => Some(V_XBGH(vd, vs1, vs2)),
        45 => Some(V_GHFN(vd, vs1, vs2)),
        34 => Some(V_VRJR(vd, vs1, vs2)),
        7 => Some(I_JRKX(vd, imm)),
        60 => Some(M_CYFN(vd, vs1)),
        36 => Some(L_PLTK(vd, vs2)),
        16 => Some(R_TKZW(vd)),
        53 => Some(B_XBKX(vs1, vs2)),
        0 => Some(J_MZQN(inst)),
        _ => None()
    }
}
val LicenseCascade_4vISA_gfmul : (bits(8), bits(8)) -> bits(8)
function LicenseCascade_4vISA_gfmul(a, b) = {
    let p1 = if a[0] == bitzero then 0x00 else b;
    let b2 = (b << 1) ^ (if b[7] == bitone then 0x1B else 0x00);
    p1 ^ (if a[1] == bitzero then 0x00 else b2)
}
val LicenseCascade_4vISA_execute : LicenseCascade_4vISA_ast -> unit
function LicenseCascade_4vISA_execute(op) = {
    match op {
        V_BFVR(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] + LicenseCascade_4vISA_R[vs2],
        V_MZMZ(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] - LicenseCascade_4vISA_R[vs2],
        V_QNQN(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] * LicenseCascade_4vISA_R[vs2],
        V_DSGH(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] ^ LicenseCascade_4vISA_R[vs2],
        V_GHZW(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] & LicenseCascade_4vISA_R[vs2],
        V_XBGH(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] | LicenseCascade_4vISA_R[vs2],
        V_GHFN(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] << unsigned(LicenseCascade_4vISA_R[vs2]),
        V_VRJR(vd, vs1, vs2) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] >> unsigned(LicenseCascade_4vISA_R[vs2]),
        I_JRKX(vd, imm) => LicenseCascade_4vISA_R[vd] = EXTZ(imm) ^ (EXTZ(imm) << 15),
        M_CYFN(vd, vs1) => LicenseCascade_4vISA_R[vd] = LicenseCascade_4vISA_R[vs1] ^ 0xDE910000,
        L_PLTK(vd, _vs2) => { () },
        R_TKZW(vd) => { () },
        B_XBKX(vs1, vs2) => { () },
        J_MZQN(tgt) => { LicenseCascade_4vISA_PC = tgt },
    }
}
