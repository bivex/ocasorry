(* Sail ISA: VCPU1_Arch  tier=1  imm12  GF_poly=0xA3  ROL=11
*  Generated by OcaSorry Polymorphic ISA Synthesizer *)
default Order dec
$include <prelude.sail>
type regidx_VCPU1_Arch = bits(5)
type imm_VCPU1_Arch    = bits(12)
register VCPU1_Arch_R  : vector(16, dec, bits(32))
register VCPU1_Arch_PC : bits(32)
union VCPU1_Arch_ast = {
    V_TKDS : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_CYFN : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_ZWVR : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_TKWT : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_TKVR : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_KXJR : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_GHJR : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    V_FNQN : (regidx_VCPU1_Arch, regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    I_FNCY : (regidx_VCPU1_Arch, imm_VCPU1_Arch),
    M_QNPL : (regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    L_ZWCY : (regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    R_FNHP : (regidx_VCPU1_Arch),
    B_CYVR : (regidx_VCPU1_Arch, regidx_VCPU1_Arch),
    J_HPWT : (bits(32)),
}
val VCPU1_Arch_decode : bits(32) -> option(VCPU1_Arch_ast)
function VCPU1_Arch_decode(inst) = {
    let f6  : bits(6) = inst[31..26];
    let vs2 : bits(5) = inst[24..20];
    let vs1 : bits(5) = inst[19..15];
    let vd  : bits(5) = inst[11..7];
    let imm : bits(12) = inst[11..0];
    match f6 {
        31 => Some(V_TKDS(vd, vs1, vs2)),
        4 => Some(V_CYFN(vd, vs1, vs2)),
        41 => Some(V_ZWVR(vd, vs1, vs2)),
        21 => Some(V_TKWT(vd, vs1, vs2)),
        19 => Some(V_TKVR(vd, vs1, vs2)),
        6 => Some(V_KXJR(vd, vs1, vs2)),
        52 => Some(V_GHJR(vd, vs1, vs2)),
        16 => Some(V_FNQN(vd, vs1, vs2)),
        10 => Some(I_FNCY(vd, imm)),
        38 => Some(M_QNPL(vd, vs1)),
        18 => Some(L_ZWCY(vd, vs2)),
        42 => Some(R_FNHP(vd)),
        43 => Some(B_CYVR(vs1, vs2)),
        39 => Some(J_HPWT(inst)),
        _ => None()
    }
}
val VCPU1_Arch_gfmul : (bits(8), bits(8)) -> bits(8)
function VCPU1_Arch_gfmul(a, b) = {
    let p1 = if a[0] == bitzero then 0x00 else b;
    let b2 = (b << 1) ^ (if b[7] == bitone then 0xA3 else 0x00);
    p1 ^ (if a[1] == bitzero then 0x00 else b2)
}
val VCPU1_Arch_execute : VCPU1_Arch_ast -> unit
function VCPU1_Arch_execute(op) = {
    match op {
        V_TKDS(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] + VCPU1_Arch_R[vs2],
        V_CYFN(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] - VCPU1_Arch_R[vs2],
        V_ZWVR(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] * VCPU1_Arch_R[vs2],
        V_TKWT(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] ^ VCPU1_Arch_R[vs2],
        V_TKVR(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] & VCPU1_Arch_R[vs2],
        V_KXJR(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] | VCPU1_Arch_R[vs2],
        V_GHJR(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] << unsigned(VCPU1_Arch_R[vs2]),
        V_FNQN(vd, vs1, vs2) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] >> unsigned(VCPU1_Arch_R[vs2]),
        I_FNCY(vd, imm) => VCPU1_Arch_R[vd] = EXTZ(imm) ^ (EXTZ(imm) << 11),
        M_QNPL(vd, vs1) => VCPU1_Arch_R[vd] = VCPU1_Arch_R[vs1] ^ 0xDE8B0000,
        L_ZWCY(vd, _vs2) => { () },
        R_FNHP(vd) => { () },
        B_CYVR(vs1, vs2) => { () },
        J_HPWT(tgt) => { VCPU1_Arch_PC = tgt },
    }
}
