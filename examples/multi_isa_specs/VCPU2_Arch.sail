(* Sail ISA: VCPU2_Arch  tier=1  imm16  GF_poly=0x8D  ROL=11
*  Generated by Vectis Polymorphic ISA Synthesizer *)
default Order dec
$include <prelude.sail>
type regidx_VCPU2_Arch = bits(5)
type imm_VCPU2_Arch    = bits(16)
register VCPU2_Arch_R  : vector(16, dec, bits(32))
register VCPU2_Arch_PC : bits(32)
union VCPU2_Arch_ast = {
    V_JRKX : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_KXMZ : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_FNZW : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_MZGH : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_VRQN : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_WTTK : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_GHDS : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    V_JRKX : (regidx_VCPU2_Arch, regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    I_HPXB : (regidx_VCPU2_Arch, imm_VCPU2_Arch),
    M_JRMZ : (regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    L_WTVR : (regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    R_HPVR : (regidx_VCPU2_Arch),
    B_GHTK : (regidx_VCPU2_Arch, regidx_VCPU2_Arch),
    J_JRZW : (bits(32)),
}
val VCPU2_Arch_decode : bits(32) -> option(VCPU2_Arch_ast)
function VCPU2_Arch_decode(inst) = {
    let f6  : bits(6) = inst[31..26];
    let vs2 : bits(5) = inst[24..20];
    let vs1 : bits(5) = inst[19..15];
    let vd  : bits(5) = inst[11..7];
    let imm : bits(16) = inst[15..0];
    match f6 {
        51 => Some(V_JRKX(vd, vs1, vs2)),
        46 => Some(V_KXMZ(vd, vs1, vs2)),
        22 => Some(V_FNZW(vd, vs1, vs2)),
        48 => Some(V_MZGH(vd, vs1, vs2)),
        17 => Some(V_VRQN(vd, vs1, vs2)),
        4 => Some(V_WTTK(vd, vs1, vs2)),
        3 => Some(V_GHDS(vd, vs1, vs2)),
        50 => Some(V_JRKX(vd, vs1, vs2)),
        1 => Some(I_HPXB(vd, imm)),
        29 => Some(M_JRMZ(vd, vs1)),
        31 => Some(L_WTVR(vd, vs2)),
        16 => Some(R_HPVR(vd)),
        62 => Some(B_GHTK(vs1, vs2)),
        38 => Some(J_JRZW(inst)),
        _ => None()
    }
}
val VCPU2_Arch_gfmul : (bits(8), bits(8)) -> bits(8)
function VCPU2_Arch_gfmul(a, b) = {
    let p1 = if a[0] == bitzero then 0x00 else b;
    let b2 = (b << 1) ^ (if b[7] == bitone then 0x8D else 0x00);
    p1 ^ (if a[1] == bitzero then 0x00 else b2)
}
val VCPU2_Arch_execute : VCPU2_Arch_ast -> unit
function VCPU2_Arch_execute(op) = {
    match op {
        V_JRKX(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] + VCPU2_Arch_R[vs2],
        V_KXMZ(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] - VCPU2_Arch_R[vs2],
        V_FNZW(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] * VCPU2_Arch_R[vs2],
        V_MZGH(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] ^ VCPU2_Arch_R[vs2],
        V_VRQN(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] & VCPU2_Arch_R[vs2],
        V_WTTK(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] | VCPU2_Arch_R[vs2],
        V_GHDS(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] << unsigned(VCPU2_Arch_R[vs2]),
        V_JRKX(vd, vs1, vs2) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] >> unsigned(VCPU2_Arch_R[vs2]),
        I_HPXB(vd, imm) => VCPU2_Arch_R[vd] = EXTZ(imm) ^ (EXTZ(imm) << 11),
        M_JRMZ(vd, vs1) => VCPU2_Arch_R[vd] = VCPU2_Arch_R[vs1] ^ 0xDEB00000,
        L_WTVR(vd, _vs2) => { () },
        R_HPVR(vd) => { () },
        B_GHTK(vs1, vs2) => { () },
        J_JRZW(tgt) => { VCPU2_Arch_PC = tgt },
    }
}
